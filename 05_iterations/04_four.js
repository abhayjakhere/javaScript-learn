const myLangObj = {
    cpp : "C++",
    js : "JavaScript",
    rb : "Ruby",
    swift : "Swift by apple",
    py : "Python"
}

for (const key in myLangObj) {
    // console.log(`${key} shortcut is for ${myLangObj[key]}`);
}

let myarr = [1,2,3,4,5]

for (const key in myarr) {
    console.log(myarr[key]);
}